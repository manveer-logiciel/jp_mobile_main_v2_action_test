import 'package:flutter/material.dart';
class LocaleConst {
  static const Locale uk =  Locale('en', 'GB');
  static const Locale usa = Locale('en', 'US');
  static const Locale spain = Locale('es', 'US');
}