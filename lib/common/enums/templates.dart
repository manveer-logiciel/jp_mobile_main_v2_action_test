
enum DbElementType {
  email,
  formProposal
}

enum ProposalTemplateFormType {
  add,
  edit
}

enum TableCellType {
  head,
  body,
  foot,
}

enum EstimateTemplateFormType {
  add,
  edit
}

enum FetchTemplateType {
  proposalTemplate,
  worksheetTemplate,
  defaultTemplate
}